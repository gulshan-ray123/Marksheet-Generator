<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error Page</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: linear-gradient(45deg, #ff4e50, #f9d423);
            overflow: hidden;
        }
        
        /* Animated Background */
        @keyframes moveBackground {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }
        
        body {
            background-size: 300% 300%;
            animation: moveBackground 8s infinite alternate ease-in-out;
        }
        
        .error-container {
            text-align: center;
            background: rgba(255, 255, 255, 0.9);
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
        }
        .error-icon {
            font-size: 50px;
            color: red;
        }
        .error-title {
            font-size: 24px;
            font-weight: bold;
            color: #333;
        }
        .error-message {
            font-size: 18px;
            color: #666;
            margin: 10px 0;
        }
        .retry-button {
            display: inline-block;
            padding: 10px 20px;
            background: red;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            transition: background 0.3s;
        }
        .retry-button:hover {
            background: darkred;
        }
        
        @media (max-width: 600px) {
            .error-container {
                width: 90%;
            }
            .error-title {
                font-size: 20px;
            }
            .error-message {
                font-size: 16px;
            }
            .retry-button {
                padding: 8px 16px;
            }
        }
    </style>
</head>
<body>
    <div class="error-container">
        <div class="error-icon">❌</div>
        <div class="error-title">Oops! Something Went Wrong</div>
        <div class="error-message">We couldn't process your request. Please try again later.</div>
        <a href="/" class="retry-button">Try Again</a>
    </div>
</body>
</html>
